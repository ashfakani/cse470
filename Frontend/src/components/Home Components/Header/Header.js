import React from "react";
import NavbarCarousel from "../Navbar with Carousel/NavbarCarousel";

const Header = () => {
  return (
    <div className="header-container">
      <NavbarCarousel />
    </div>
  );
};

export default Header;
